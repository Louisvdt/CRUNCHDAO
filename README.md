# DataCrunch Equity Market Neutral

My submission for the [CrunchDAO DataCrunch Equity Market Neutral]competition.
The goal: predict a per-stock cross-sectional signal (target ∈ {-1, 0, +1, and a few continuous bins})
that ranks stocks in the Russell 3000 universe, with scoring done via **per-moon Pearson correlation**.

The repo is centered around a single notebook ([`submission.ipynb`](submission.ipynb)) that walks through
the whole pipeline: EDA → metric study → modeling → local evaluation.

## Repo layout

```
submission.ipynb        Main notebook (EDA + final model)
requirements.txt        Python deps
data/                   Datasets (gitignored, downloaded by `crunch setup-notebook`)
resources/              Trained models (gitignored, regenerated by running the notebook)
prediction/             Crunch CLI test outputs (gitignored)
.crunchdao/             Crunch CLI project config (token gitignored)
```

## How to run

```bash
# 1. Create a virtualenv and install deps
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# 2. Set up the Crunch CLI (replace <YOUR_TOKEN> with yours)
#    Token available at https://www.crunchdao.com (Account > API tokens)
crunch setup-notebook datacrunch-2 <YOUR_TOKEN>

# 3. Open the notebook and run all cells
jupyter notebook submission.ipynb
```

The first run will download the reduced dataset (~a few hundred MB) into `data/`, train the model,
and write it to `resources/model.joblib`.

## Notebook structure

The notebook is split into four phases. Each phase reads on its own:

### Phase A — Exploratory Data Analysis
Understand the data before modeling. Key sub-sections:

- **A.1** — Shape, missing values, target distribution. The target is mostly 0 with rare ±1 movers (5–25% of stocks per moon depending on regime).
- **A.2** — Feature nature. All 1150 features are pre-encoded as septiles per moon. Two interesting clusters surface:
  - **"Stars"** (~15 features): perfectly monotone (|Spearman ρ|=1.0) with strong signal (range ≈ 0.04). Almost the same underlying information — likely a reversal cluster.
  - **"Gems"** (~15 features): non-monotone but strong signal. Their shape is a "reversal at extremes" — flat in the middle septiles, sharp drop at the top or bottom.
- **A.3** — Temporal analysis. Universe size 1700–2600 stocks/moon, strong regime persistence (volatility is autocorrelated), feature drift is real but modest — most top features remain predictive across decades.
- **A.4** — Linear (IC sharpe) vs non-linear (Mutual Information) signal. The two metrics surface **disjoint** feature families: IC catches the linear reversal cluster, MI catches the "extreme reversal" non-linear features. Hierarchical clustering on the 1150×1150 Spearman matrix shows clear duplicate families: at corr ≥ 0.95, ~30% of features are deduplicable.

### Phase B — Metric & validation
- **B.1** — Monte-Carlo simulator of the Pearson metric. Confirms that sign accuracy on movers dominates the score.

> *Walk-forward validation (purged K-fold) was used during development to score model candidates but is omitted from this clean notebook.*

### Phase C — Modeling
- **C.1** — Feature engineering. The notebook keeps the **"extreme reversal" binary features** (C.1.2): aggregated counters built from the top-MI features. Empirically the gain was marginal and the final model below does not include them, but the code is left in for reference.
- **C.3** — Final model: LightGBM regressor with hyperparameters found via Optuna on a 3-fold walk-forward (Optuna search omitted from the clean notebook). The `train()` / `infer()` functions match the CrunchDAO contract.

### Phase D — Local evaluation
- Run `crunch_tools.test()`, compute per-moon Pearson, plot per-moon score and per-regime breakdown, inspect feature importance (gain + cumulative).

## What was removed from this clean notebook

To keep the repo focused, the following development-time sections were dropped from `submission.ipynb`:

- **B.2** — Walk-forward validation harness (`walk_forward_score`, baseline LGBM run).
- **C.1.1** — Family aggregation features (negative result: no measurable gain).
- **C.2.1** — Optuna hyperparameter search (the best params it found are hardcoded in the final `train()`).

## Final result

The best LGBM configuration reaches a **mean per-moon Pearson around 0.03–0.035** in walk-forward
on the 5-fold purged CV used during development, in line with what the simulator in B.1 says is achievable
given the ternary target. Concrete numbers from the most recent local run are visible in Phase D of the notebook.

## Stack

- Python 3.11, LightGBM, scikit-learn 1.7.2, pandas, numpy
- Visualization: matplotlib + seaborn
- Stats: scipy, statsmodels
- Competition framework: `crunch-cli`

See [`requirements.txt`](requirements.txt) for the full list.

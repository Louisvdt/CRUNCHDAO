.venv/
.crunchdao/token

__pycache__/
*.pyc

# Datasets (downloaded via `crunch setup-notebook`)
data/*.parquet
data/*.csv

# Trained model artefacts (regenerated by re-running the notebook)
resources/*.joblib

# Local-only working notebook (clean version is submission.ipynb)
notebook.ipynb

# Crunch CLI outputs
prediction/
